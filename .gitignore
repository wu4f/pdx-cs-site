credentials.json
token.json
.admin_token
*.pyc
__pycache__
.venv
logs/
# Generated site pages (rebuilt by `cspdx build` from the Google Docs
# tab/Heading-1 splitters). The section data (build/sections.json) stays
# tracked; the rendered HTML does not.
build/site/*
!build/site/index.html
# Runtime build metadata (timestamps + per-doc revisions); changes every build.
build/build_meta.json
